{
  "name": "digital-sat-sprint-sprint39-score-engine-2",
  "version": "0.7.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint . --ignore-pattern .next/** --ignore-pattern node_modules/** --ignore-pattern playwright-report/** --ignore-pattern test-results/** --ignore-pattern blob-report/**",
    "lint:fix": "eslint . --fix --ignore-pattern .next/** --ignore-pattern node_modules/** --ignore-pattern playwright-report/** --ignore-pattern test-results/** --ignore-pattern blob-report/**",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:headed": "playwright test --headed",
    "test:e2e:report": "playwright show-report"
  },
  "dependencies": {
    "next": "^15.5.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "zustand": "^5.0.8"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/node": "^20.17.0",
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "eslint": "9.39.5",
    "eslint-config-next": "16.2.12",
    "typescript": "^5.8.0"
  }
}
