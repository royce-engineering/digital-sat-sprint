"use client";
import Link from "next/link";
import { useEffect,useMemo,useState } from "react";
import { useSearchParams } from "next/navigation";
import { isEquivalentAnswer } from "@/lib/adaptive/answerEvaluator";
import { buildMistakeRecords,targetedQuestions,updateProgress,type ProgressMap } from "@/lib/mistakes/mistakeEngine";
import { useAdaptiveExamStore } from "@/store/adaptiveExamStore";
const KEY="digital-sat-sprint-mistake-progress";
export default function TargetedPractice(){const params=useSearchParams();const skill=params.get("skill")||undefined;const focus=params.get("focus");const attempts=useAdaptiveExamStore(s=>s.attempts);const [progress,setProgress]=useState<ProgressMap>({});const [index,setIndex]=useState(0);const [selected,setSelected]=useState(-1);const [typed,setTyped]=useState("");const [submitted,setSubmitted]=useState(false);useEffect(()=>{try{setProgress(JSON.parse(localStorage.getItem(KEY)||"{}"))}catch{}},[]);const records=useMemo(()=>buildMistakeRecords(attempts,progress),[attempts,progress]);let qs=useMemo(()=>targetedQuestions(records,skill,undefined,10),[records,skill]);if(focus){const i=qs.findIndex(q=>q.examId===focus);if(i>0)qs=[qs[i],...qs.slice(0,i),...qs.slice(i+1)];}const q=qs[index];if(!q)return <main className="pageShell"><div className="container pageContent examStart"><h1>No targeted questions found.</h1><Link className="button buttonPrimary" href="/mistakes">Open mistake notebook</Link></div></main>;const correct=q.questionType==="student-response"?isEquivalentAnswer(typed,q.numericAnswer??""):selected===q.answer;const submit=()=>{setSubmitted(true);const next=updateProgress(progress,q.examId,correct);setProgress(next);localStorage.setItem(KEY,JSON.stringify(next));};const next=()=>{setIndex(i=>Math.min(i+1,qs.length-1));setSelected(-1);setTyped("");setSubmitted(false)};return <main className="pageShell"><section className="pageHero"><div className="container"><p className="eyebrow">Targeted practice</p><h1>{skill||"Mistake recovery"}</h1><p>Question {index+1} of {qs.length} · {q.section} · {q.difficulty}</p></div></section><div className="container pageContent"><article className="courseQuestionCard">{q.passage&&<p className="coursePassage">{q.passage}</p>}<h2>{q.prompt}</h2>{q.tableData&&<table className="mathTable"><thead><tr>{q.tableData.headers.map(h=><th key={h}>{h}</th>)}</tr></thead><tbody>{q.tableData.rows.map((row,i)=><tr key={i}>{row.map((c,j)=><td key={j}>{c}</td>)}</tr>)}</tbody></table>}{q.questionType==="student-response"?<input className="studentResponseInput" value={typed} onChange={e=>setTyped(e.target.value)} placeholder="Enter your answer" disabled={submitted}/>:<div className="courseChoiceList">{q.choices?.map((c,i)=><button type="button" className={`courseChoice ${selected===i?"selectedChoice":""} ${submitted&&i===q.answer?"correctChoice":""} ${submitted&&selected===i&&i!==q.answer?"incorrectChoice":""}`} key={i} onClick={()=>!submitted&&setSelected(i)}><strong>{String.fromCharCode(65+i)}.</strong> {c.text}</button>)}</div>}{submitted&&<div className={correct?"practiceFeedback correct":"practiceFeedback incorrect"}><h3>{correct?"Correct — mastery progress increased.":"Not yet — this question remains in review."}</h3><p>{q.walkthrough?.join(" ") || q.choices?.[q.answer]?.rationale || "Review the underlying skill and solve the problem again without relying on the previous answer."}</p></div>}</article><div className="examControls"><Link className="button buttonGhost" href="/mistakes">Back to notebook</Link>{!submitted?<button className="button buttonPrimary" onClick={submit} disabled={q.questionType==="student-response"?!typed.trim():selected<0}>Check answer</button>:index<qs.length-1?<button className="button buttonPrimary" onClick={next}>Next question</button>:<Link className="button buttonPrimary" href="/analytics">View analytics</Link>}</div></div></main>}
